<ul>
  <% names.forEach((name) => { %>
    <li>
      <svg class="tw-icon">
        <use xlink:href="#<%= name %>"></use>
      </svg>
      <small>
        <%= name %>
      </small>
    </li>
  <% }); %>
</ul>